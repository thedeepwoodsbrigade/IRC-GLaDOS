development:
  sessions:
    default:
      database: ircglados_development
      hosts:
        - localhost:27017
      options:
        consistency: :strong
  options:

test:
  sessions:
    default:
      database: ircglados_test
      hosts:
        - localhost:27017
      options:
        consistency: :strong
  options:

production:
  sessions:
    default:
      database: ircglados
      hosts:
        - localhost:27017
      options:
        consistency: :strong
  options:

